package server

// authenticate will call server's endpoint with user's credentials to retrieve JWT token; return JWT.
func authenticate(phoneNumber, password string) (string, error) {
	panic("myFunction is not implemented yet")
}
